/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'upload.wikimedia.org',
        'assets.vercel.com',
        'render.com',
        'www.netlify.com',
        'github.com',
      ],
    },
  };
  
  export default nextConfig;